<p style="color: green"><%= notice %></p>

<%= render "ui/components/breadcrumbs",
    breadcrumbs: projects_breadcrumbs(page_code: :index),
    current_page_name: "Projects" %>

<%= render "ui/elements/title", title: "Projects" %>

<%= render "ui/components/actions", links: [
    link_to("New", new_project_path)
] %>

<%= render "ui/components/simple_cards_list", values: projects_simple_card_values(@projects) %>
